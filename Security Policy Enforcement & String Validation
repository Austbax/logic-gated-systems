Password checker

"""

password = input("please enter your password: ")
char = ("!", "@", "#", "$","%","&")
num = ["1","2", "3", "4", "5","6","7","8","9"]
if len(password) > 6 and len(password) < 12:
 num = any(b in password  for b in num)
 char = any(c in password for c in char)
 if num and char:
    print("password ok")
 else:
    if not char:
      print("password needs special character")
    if not num:
      print("passwords needs at least one number")
else:
   print("password needs to be between 6 and 12 characters")
print(f"your password length is {len(password)} characters long")
